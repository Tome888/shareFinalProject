1. open the server with terminal and run the command (json-server --watch db.json --port 5000)

2.Endpoints are
{
http://localhost:5000/events

===FILTER EVENTS BY ID===
http://localhost:5000/events?id=3
}